
peakenergy.m:

function [ vector] = peakenergy( matrix )

[rows, columns] = size(matrix);

max=0;
for i = 1:rows
    for j = 1:columns
        if matrix(i, j)> max
            max = matrix(i, j);
        end
        
    end
    
    vector(i) = max;
    
end

end
